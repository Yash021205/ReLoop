export const intercept = (data) => Promise.resolve({});

export const inspect = (data) => Promise.resolve({});

export const questions = (data) => Promise.resolve({});

export const verify = (data) => Promise.resolve({});

export const triage = (data) => Promise.resolve({});

export const explain = (data) => Promise.resolve({});
